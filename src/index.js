import CodeGenerator from './CodeGenerator';
 /**
   * 根据 type和 code 的值生成base64ImageStrng
   *
   * @static
   * @param {Obj} { type = Type.Code128, code }
   * @returns {string} base64ImageStrng
   * @memberof CodeGenerator
   */
export default CodeGenerator;
